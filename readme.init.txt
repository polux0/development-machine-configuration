Steps to create development enviorment virtual machine - these steps are abstract, just to create concept, but will be supported with script per these abstract steps;

Pre-condition is to create initial bash script which will enable execution of other scripts in current directorium. Pre-condition script needs to be activated manually ( chmod u+x ); `initial-script.sh` will fire up other ones ( Must be launched from current directorium );


Software to be aquired by scripts: 

##sudo apt-get install curl
1. git -> Create git repository for git options?
2. sublime text *note -> Create git repository for sublime text options
3. intelliJIdea - > -||-

